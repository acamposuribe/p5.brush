# Standalone Adapter

This folder is reserved for the future non-p5 runtime adapter.

The goal is to provide the same rendering/runtime services that the current
`src/adapters/p5/` layer provides, but without requiring p5.
